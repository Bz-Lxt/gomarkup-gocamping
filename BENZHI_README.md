# GoCamping 评测说明

本项目是基于Go语言的户外徒步、小众露营地位置共享与离线足迹防迷路安全枢纽（Mini 驴友足迹），旨在解决户外徒步、小众露营地位置共享与离线足迹防迷路安全枢纽（Mini 驴友足迹）相关的工程问题，使用了Go、Vue 3、Mapbox、Leaflet，功能有小众露营路书编辑器、队员实时时空位置墙、基于地理信息（GIS）的空间格栅匹配、断线重连与增量轨迹合并。

Go 模块位于 `backend/`。评测入口：在该目录执行 `go test ./...`。
